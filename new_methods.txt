
new game(element)
game.addSheet(filepath, alias, cellWidth, cellHeight)
game.addJSON(filepath, alias)
game.bind(keyName, keyCode, [keyCode, ...])
game.activateTouch()
game.setScreenMode(...)
game.start(view)
// game.stop()

(view).update({
  viewClock
  gameClock
  buttons
  screen
})
(view).buttondown(button)
(view).buttonup(button)
(view).keypress(keypressEvt)
// (view).touch
// (view).drag
// (view).release

screen.element
screen.canvas
screen.context
screen.mode
screen.minWidth
screen.minHeight
screen.width
screen.height
screen.left
screen.top
screen.right
screen.bottom
screen.drawScale
screen.wasResized
screen.prevDims

screen.reset()
screen.clear()
screen.clear(x, y, w, h)
screen.fill(color)
screen.fill(color, x, y, w, h)
screen.blit(sheet, frame, x, y)
screen.write(text, x, y, {align, cols, rows})
// screen.box(text, x, y, {align, cols, rows, width, height})
// screen.getPanel(x, y, width, height, [xAnchor, yAnchor])

screen.origin()
screen.origin(x, y)
screen.offset()
screen.offset(x, y)
screen.setFont(sheetName)
screen.getFontSheet()

screen.setMode('adapt')
screen.setMode('adapt', scale)
  screen.setScale(scale)
screen.setMode('fixed')
screen.setMode('fixed', scale)
screen.setMode('fixed', width, height, scale)
  screen.setSize(width, height)
  screen.setScale(scale)
// screen.setMode('scale')
// screen.setMode('scale', width, height)
  // screen.setSize(width, height)
screen.setMode('scale-overflow')
screen.setMode('scale-overflow', width, height)
  screen.setSize(width, height)
  // screen.setMaxOverflow(width, height)