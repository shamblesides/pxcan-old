
new game(element)
game.addSheet(filepath, alias, cellWidth, cellHeight)
game.addJSON(filepath, alias)
game.bind(keyName, keyCode, [keyCode, ...])
game.activateTouch()
game.activateKeyboard()
game.setFrameSkip(frameSkip)
game.setScreenMode(...)
game.setScreenSize(...)
game.setScreenScale(...)
game.start(view)
// game.stop()

(view).update({
  viewClock
  gameClock
  buttons
  screen
})
(view).buttondown(button)
(view).buttonup(button)
(view).keypress(keypressEvt)
// (view).touch
// (view).drag
// (view).release

screen.element
screen.canvas
screen.context

screen.left
screen.top
screen.right
screen.bottom
screen.width
screen.height
screen.drawScale

screen.font

screen.reset()
screen.clear()
screen.clear(x, y, w, h)
screen.clear(x, y, w, h, xAnchor, yAnchor)
screen.fill(color)
screen.fill(color, x, y, w, h)
screen.fill(color, x, y, w, h, xAnchor, yAnchor)
screen.blit(sheet, frame, x, y)
screen.blit(sheet, frame, flip, x, y)
screen.blit(sheet, frame, x, y, xAnchor, yAnchor)
screen.blit(sheet, frame, flip, x, y, xAnchor, yAnchor)
screen.write(text, x, y, {align, cols, rows})
// screen.box(text, x, y, {align, cols, rows, width, height})
// screen.getPanel(x, y, width, height, [xAnchor, yAnchor])

screen.origin()
screen.origin(x, y)
screen.offset()
screen.offset(x, y)

screen.mode()
screen.mode('adapt')
screen.mode('adapt', scale)
  screen.setScale(scale)
screen.mode('fixed')
screen.mode('fixed', scale)
screen.mode('fixed', width, height, scale)
  screen.setSize(width, height)
  screen.setScale(scale)
// screen.mode('scale')
// screen.mode('scale', width, height)
  // screen.setSize(width, height)
screen.mode('scale-overflow')
screen.mode('scale-overflow', width, height)
  screen.setSize(width, height)
  // screen.setMaxOverflow(width, height)

screen.fitElement()